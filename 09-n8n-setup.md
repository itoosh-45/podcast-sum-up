# ישיבה 2 — המכונה הידנית ב-n8n

הפעלה ידנית אחת: לוחצים "Execute", ומקבלים מייל עם הסיכום והתמלול מצורף.

---

## מה רץ במחשב

n8n מותקנת גלובלית דרך npm. להפעלה:

```
n8n start
```

ואז בדפדפן: **http://localhost:5678**

בהרצה הראשונה n8n מבקשת ליצור משתמש מקומי. זה חשבון על המחשב שלך בלבד — לא נשלח לשום מקום, ולא דורש כרטיס אשראי.

לעצירה: `Ctrl+C` בחלון שבו הרצת.

---

## התרחיש — 11 תחנות

```
Manual Trigger
   ↓
Setup              כל הערכים שמשתנים: קישור, כותרת, גודל, מודל, שני הפרומפטים
   ↓
Start Upload       פותח העלאה ל-Gemini, מקבל כתובת העלאה חד-פעמית
   ↓
Download Episode   מוריד את ה-MP3 (82MB) — כאן Make החינמית נשברה
   ↓
Finish Upload      שולח את הבתים עצמם, מקבל file_uri
   ↓
Transcribe         קריאה 1 — תמלול מלא
   ↓
Summarize          קריאה 2 — תשעת הפריטים
   ↓
Collect            אוסף כותרת, תמלול וסיכום למקום אחד
   ↓
Summary To HTML    מרקדאון → HTML למייל
   ↓
Transcript To File תמלול → transcript.txt
   ↓
Send Email         מייל עם הסיכום בגוף והתמלול מצורף
```

### למה ההורדה באה *אחרי* פתיחת ההעלאה

זה נראה הפוך, ויש לזה סיבה. ב-n8n כל תחנה מחליפה את הפריט שעובר בצינור, כולל הקובץ הבינארי שנמצא עליו. אם היינו מורידים קודם, הקובץ היה נמחק בתחנה שאחריה.

לכן קודם פותחים את ההעלאה (בקשה קטנה, JSON בלבד), ורק אז מורידים — כך שהקובץ יושב על הפריט בדיוק כשהוא נכנס לתחנת ההעלאה. את כתובת ההעלאה שואבים אחורה בביטוי `$('Start Upload')`.

זה הדפוס המרכזי ב-n8n ששווה להבין: **הזרימה היא קו אחד, אבל אפשר תמיד לשלוח יד אחורה ולקחת ערך מתחנה קודמת.**

---

## שני הדברים שאתה צריך להגדיר בעצמך

> **אל תדביק לי כאן אף אחד מהם.** שניהם נכנסים ישירות ל-n8n.

### 1. מפתח Gemini API

- ב-[aistudio.google.com/apikey](https://aistudio.google.com/apikey) → **Create API key**
- ב-n8n: **Credentials → New → Header Auth**
  - Name: `x-goog-api-key`
  - Value: המפתח
- שמור בשם `Gemini`

שלוש תחנות משתמשות בו: Start Upload · Transcribe · Summarize.
תחנת Finish Upload לא צריכה אותו — כתובת ההעלאה שקיבלנו כבר מאושרת מראש.

### 2. שליחת מייל

הדרך הקצרה היא SMTP עם סיסמת אפליקציה, ולא חיבור OAuth של Gmail — שדורש פרויקט ב-Google Cloud ומסך הרשאות.

- ב-[myaccount.google.com/apppasswords](https://myaccount.google.com/apppasswords) → צור סיסמת אפליקציה (דורש אימות דו-שלבי פעיל)
- ב-n8n: **Credentials → New → SMTP**
  - Host: `smtp.gmail.com` · Port: `465` · SSL: פעיל
  - User: הכתובת שלך · Password: סיסמת האפליקציה

#### שולח מול נמען — לא אותו דבר

**השולח לא באמת חופשי לבחירה.** SMTP של Gmail שולח אך ורק מהחשבון שעליו נוצרה סיסמת האפליקציה; אי אפשר לשלוח בשם כתובת אחרת. אז השולח נקבע ברגע שבחרת באיזה חשבון לייצר את הסיסמה.

**הנמען חופשי לגמרי** — כל כתובת.

שניהם יושבים בראש `build_workflow.py` כשני קבועים, `SENDER` ו-`RECIPIENT`. אם שניהם אותה כתובת, המייל פשוט נשלח אליך מעצמך — וזה בסדר גמור.

#### ואם תיבת הדואר שלך היא Proton

**Proton לא יכול להיות השולח**, אלא אם יש מנוי בתשלום *וגם* דומיין מותאם אישית:

| | |
|---|---|
| Proton חינמי | אין SMTP כלל — לא Bridge ולא submission |
| Proton בתשלום | SMTP קיים, אך מותנה בדומיין מותאם אישית |

**וזה לא מפריע לנו,** כי השולח הוא רק אמצעי ההובלה — לא המקום שבו קוראים. חשבון ג'ימייל נפרד משמש כמשאית משלוחים שלא פותחים אף פעם, ו-`RECIPIENT` מכוון לכתובת הפרוטון. הסיכומים נוחתים שם ונקראים שם. פרוטון רק מקבל, וקבלה עובדת מצוין גם בחינמי.

---

## ההרצה

1. `n8n start` → פתח את http://localhost:5678
2. **Workflows → ⋯ → Import from File** → בחר `n8n-workflow.json`
3. פתח כל אחת משלוש תחנות ה-Gemini ובחר את ה-Credential שיצרת, ואת ה-SMTP בתחנת המייל
4. **Execute workflow**

הרצה שלמה לוקחת בערך **8 עד 12 דקות** — רובן בתמלול.

### מה לצפות לו

- תחנת ההורדה תיקח דקה-שתיים על 82MB
- תחנת Transcribe מוגדרת ל-5 ניסיונות עם המתנה של 20 שניות ביניהם. זה מכוון: אחרי ההעלאה Gemini עדיין מעבד את הקובץ לכמה שניות ומחזיר שגיאה למי שמבקש אותו מוקדם מדי. הניסיון החוזר פותר את זה לבד
- אם תחנה נכשלת, לחיצה עליה מראה את הבקשה והתשובה המלאות — שם מתחיל הדיבאג

---

## לשנות פרק

`n8n-workflow.json` נבנה מהפרומפטים שב-`prompts/`, כדי שלא תהיינה שתי גרסאות שנפרדות זו מזו.

אחרי עריכת פרומפט, או להחלפת הפרק:

```
python latest.py חושבים-טוב        # לקחת קישור וגודל בבתים
python build_workflow.py           # לבנות מחדש
```

ואז לייבא שוב ב-n8n. את הקישור, הכותרת והגודל עורכים בראש `build_workflow.py`.
