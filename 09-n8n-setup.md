# ישיבה 2 — המכונה הידנית ב-n8n

הפעלה ידנית אחת: לוחצים "Execute", ומקבלים מייל עם הסיכום והתמלול מצורף.

---

## מה רץ במחשב

n8n מותקנת גלובלית דרך npm. להפעלה:

```
n8n start
```

ואז בדפדפן: **http://localhost:5678**

בהרצה הראשונה n8n מבקשת ליצור משתמש מקומי. זה חשבון על המחשב שלך בלבד — לא נשלח לשום מקום, ולא דורש כרטיס אשראי.

לעצירה: `Ctrl+C` בחלון שבו הרצת.

---

## התרחיש — 11 תחנות

```
Manual Trigger
   ↓
Setup              כל הערכים שמשתנים: קישור, כותרת, גודל, מודל, שני הפרומפטים
   ↓
Start Upload       פותח העלאה ל-Gemini, מקבל כתובת העלאה חד-פעמית
   ↓
Download Episode   מוריד את ה-MP3 (82MB) — כאן Make החינמית נשברה
   ↓
Finish Upload      שולח את הבתים עצמם, מקבל file_uri
   ↓
Transcribe         קריאה 1 — תמלול מלא
   ↓
Summarize          קריאה 2 — תשעת הפריטים
   ↓
Collect            אוסף כותרת, תמלול וסיכום למקום אחד
   ↓
Summary To HTML    מרקדאון → HTML למייל
   ↓
Transcript To File תמלול → transcript.txt
   ↓
Send Email         מייל עם הסיכום בגוף והתמלול מצורף
```

### למה ההורדה באה *אחרי* פתיחת ההעלאה

זה נראה הפוך, ויש לזה סיבה. ב-n8n כל תחנה מחליפה את הפריט שעובר בצינור, כולל הקובץ הבינארי שנמצא עליו. אם היינו מורידים קודם, הקובץ היה נמחק בתחנה שאחריה.

לכן קודם פותחים את ההעלאה (בקשה קטנה, JSON בלבד), ורק אז מורידים — כך שהקובץ יושב על הפריט בדיוק כשהוא נכנס לתחנת ההעלאה. את כתובת ההעלאה שואבים אחורה בביטוי `$('Start Upload')`.

זה הדפוס המרכזי ב-n8n ששווה להבין: **הזרימה היא קו אחד, אבל אפשר תמיד לשלוח יד אחורה ולקחת ערך מתחנה קודמת.**

---

## שני הדברים שאתה צריך להגדיר בעצמך

> **אל תדביק לי כאן אף אחד מהם.** שניהם נכנסים ישירות ל-n8n.

### 1. מפתח Gemini API

- ב-[aistudio.google.com/apikey](https://aistudio.google.com/apikey) → **Create API key**
- ב-n8n: **Credentials → New → Header Auth**
  - Name: `x-goog-api-key`
  - Value: המפתח
- שמור בשם `Gemini`

שלוש תחנות משתמשות בו: Start Upload · Transcribe · Summarize.
תחנת Finish Upload לא צריכה אותו — כתובת ההעלאה שקיבלנו כבר מאושרת מראש.

### 2. שליחת מייל

הדרך הקצרה היא SMTP עם סיסמת אפליקציה, ולא חיבור OAuth של Gmail — שדורש פרויקט ב-Google Cloud ומסך הרשאות.

- ב-[myaccount.google.com/apppasswords](https://myaccount.google.com/apppasswords) → צור סיסמת אפליקציה (דורש אימות דו-שלבי פעיל)
- ב-n8n: **Credentials → New → SMTP**
  - Host: `smtp.gmail.com` · Port: `465` · SSL: פעיל
  - User: הכתובת שלך · Password: סיסמת האפליקציה

#### שולח מול נמען — לא אותו דבר

**השולח לא באמת חופשי לבחירה.** SMTP של Gmail שולח אך ורק מהחשבון שעליו נוצרה סיסמת האפליקציה; אי אפשר לשלוח בשם כתובת אחרת. אז השולח נקבע ברגע שבחרת באיזה חשבון לייצר את הסיסמה.

**הנמען חופשי לגמרי** — כל כתובת.

שתי הכתובות יושבות ב-`config.local.json`, שאינו נכנס ל-git:

```json
{
  "sender":    "כתובת מאומתת אצל ספק ה-SMTP",
  "recipient": "לכאן יגיעו הסיכומים"
}
```

**הריפו הזה ציבורי, ולכן שום כתובת אמיתית לא נכתבת בקובץ שנכנס לגיט** — לא בקוד, לא בתיעוד ולא בדוגמאות. `config.example.json` מראה את המבנה עם ערכי מקום, ומעתיקים אותו ל-`config.local.json` וממלאים.

מאותה סיבה גם `n8n-workflow.json` מוחרג מגיט: הוא נבנה מ-`config.local.json` ולכן מכיל את הכתובות. הוא קובץ שנבנה מחדש בכל הרצה, אז אין מה לשמור אותו ממילא.

#### ✅ מה נבחר בפועל

**Gmail כשולח, עם סיסמת אפליקציה.** זו הדרך הקצרה ביותר: אין חשבון חדש לפתוח, אין טופס כתובת ארגון, ואין חתימת שירות בתחתית המייל.

השולח הוא רק אמצעי ההובלה — הסיכומים נקראים בתיבה שמוגדרת כ-`recipient`, שיכולה להיות כל כתובת.

הסקירה שלמטה נשמרת כי היא עדיין רלוונטית אם אי פעם תרצה שולח שאינו גוגל, או אם Gmail יחסום את הגישה.

---

#### ואם תיבת הדואר שלך היא Proton

**Proton לא יכול להיות השולח**, אלא אם יש מנוי בתשלום *וגם* דומיין מותאם אישית:

| | |
|---|---|
| Proton חינמי | אין SMTP כלל — לא Bridge ולא submission |
| Proton בתשלום | SMTP קיים, אך מותנה בדומיין מותאם אישית |

**וזה לא מפריע לנו,** כי השולח הוא רק אמצעי ההובלה — לא המקום שבו קוראים. פרוטון רק מקבל, וקבלה עובדת מצוין גם בחינמי.

#### שולח שאינו גוגל

אין חובה לפתוח חשבון ג'ימייל. יש שתי משפחות של פתרונות:

| | מה זה | חינם | הערה |
|---|---|---|---|
| **Brevo** ⭐ | ממסר SMTP — שירות שנועד לכך שתוכנות ישלחו דרכו | 300 מיילים ליום, בלי כרטיס אשראי, ללא תפוגה | חתימת Brevo קטנה בתחתית המייל |
| Mailjet | אותו רעיון | 200 ליום | חתימה דומה |
| GMX | תיבת דואר רגילה שמאפשרת SMTP | ללא הגבלה מעשית | צריך להפעיל גישה חיצונית בהגדרות |
| ~~Outlook.com~~ | ❌ | | מיקרוסופט מכבה אימות בסיסי ל-SMTP; בחשבונות חדשים האפשרות כבר לא מופיעה |

אנחנו שולחים **כשמונה מיילים בחודש**. כל אחד מהם מכסה את זה פי מאה.

**Brevo הוא הבחירה הנכונה כאן,** ומסיבה אחת עיקרית: הוא בנוי לכך שתוכנה תשלח דרכו. תיבת דואר אישית שסקריפט מתחבר אליה שוב ושוב עלולה להיתקל בחסימות אבטחה אוטומטיות; ממסר לא.

**ויש לזה בונוס:** ב-Brevo מאמתים כתובת שולח על ידי לחיצה על קישור שנשלח אליה. כלומר אפשר לאמת את **כתובת הפרוטון עצמה** כשולח — והמייל ייראה כאילו הגיע ממך אליך, בלי שום גוגל בתמונה.

```
SENDER    = כתובת הפרוטון (מאומתת ב-Brevo)
RECIPIENT = כתובת הפרוטון
```

הגדרות ה-SMTP ל-n8n, אחרי פתיחת החשבון ב-[brevo.com](https://www.brevo.com) → **SMTP & API**:

- Host: `smtp-relay.brevo.com` · Port: `587` · STARTTLS
- User: שם המשתמש שמוצג שם · Password: **מפתח ה-SMTP** שתייצר (לא סיסמת החשבון)

##### ⚠️ Brevo דורש כתובת דואר פיזית בהרשמה

זה לא שדה שאפשר לדלג עליו, והסיבה אמיתית: Brevo היא פלטפורמת דיוור, וחוקי אנטי-ספאם מחייבים שכתובת הארגון תופיע **בתחתית כל מייל שנשלח דרכה**.

בפרויקט הזה הנמען היחיד הוא אתה עצמך, ולכן האדם היחיד שיראה את הכתובת הזו הוא אתה. מבחינת חשיפה כלפי חוץ — אפס. מה שכן, Brevo כחברה תחזיק את הכתובת שלך.

מי שמעדיף לא למסור כתובת בכלל — **GMX** היא תיבת דואר רגילה בלי המנגנון המשפטי הזה:

- הרשמה ב-[gmx.com](https://www.gmx.com), בלי כתובת ארגון ובלי חתימה בתחתית המייל
- **חובה להפעיל ידנית** גישה חיצונית בהגדרות התיבה — היא כבויה כברירת מחדל
- Host: `mail.gmx.com` · Port: `587` · STARTTLS
- ⚠️ GMX מכבה גישה חיצונית מחדש אחרי תקופה ארוכה של אי-שימוש. בקצב של פעמיים בשבוע זה לא אמור לקרות, אבל אם המערכת תושבת לחודשים — זו תהיה הסיבה הראשונה לבדוק כשהיא לא תחזור לעבוד

---

## ההרצה

1. `n8n start` → פתח את http://localhost:5678
2. **Workflows → ⋯ → Import from File** → בחר `n8n-workflow.json`
3. פתח כל אחת משלוש תחנות ה-Gemini ובחר את ה-Credential שיצרת, ואת ה-SMTP בתחנת המייל
4. **Execute workflow**

הרצה שלמה לוקחת בערך **8 עד 12 דקות** — רובן בתמלול.

### אומת מראש מול ההתקנה

לפני ההרצה הראשונה נבדק מול n8n 2.39.7 שכל 11 סוגי התחנות קיימים, שכל `typeVersion` רשום, ושכל שם שדה תואם — כדי שהייבוא לא ייפול ושלא תתגלה טעות כתיב אחרי עשר דקות של תמלול.

**הבדיקה הזו תפסה באג אחד ממשי:** בתחנת המייל, `attachments` אינו קובץ מצורף. הוא מטמיע תוכן *בתוך גוף המייל* להפניה דרך `cid:`, וזה נועד לתמונות. השדה לקובץ מצורף רגיל הוא **`fileAttachments`**. בלי התיקון המייל היה נשלח בלי התמלול, ובלי שום שגיאה שתסגיר את זה.

### מה לצפות לו

- תחנת ההורדה תיקח דקה-שתיים על 82MB
- תחנת Transcribe מוגדרת ל-5 ניסיונות עם המתנה של 20 שניות ביניהם. זה מכוון: אחרי ההעלאה Gemini עדיין מעבד את הקובץ לכמה שניות ומחזיר שגיאה למי שמבקש אותו מוקדם מדי. הניסיון החוזר פותר את זה לבד
- אם תחנה נכשלת, לחיצה עליה מראה את הבקשה והתשובה המלאות — שם מתחיל הדיבאג

---

## לשנות פרק

`n8n-workflow.json` נבנה מהפרומפטים שב-`prompts/`, כדי שלא תהיינה שתי גרסאות שנפרדות זו מזו.

אחרי עריכת פרומפט, או להחלפת הפרק:

```
python latest.py חושבים-טוב        # לקחת קישור וגודל בבתים
python build_workflow.py           # לבנות מחדש
```

ואז לייבא שוב ב-n8n. את הקישור, הכותרת והגודל עורכים בראש `build_workflow.py`.
