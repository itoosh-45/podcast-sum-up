# הוראות לפרויקט

## פרטיות — הריפו הזה ציבורי

**אסור שכתובות מייל אישיות, או כל פרט מזהה אחר, ייכנסו לקבצים שנכנסים ל-git.**
זה חל גם על תיעוד, על דוגמאות ועל הודעות קומיט — לא רק על קוד.

הכתובות האמיתיות יושבות ב-`config.local.json`, שנמצא ב-`.gitignore` ולא עוזב את המחשב.
`config.example.json` מתועד עם ערכי מקום בלבד.

`n8n-workflow.json` גם הוא ב-`.gitignore`, למרות שהוא לא נכתב ביד: הוא נבנה מ-`config.local.json`
ולכן מכיל את הכתובות. קובץ שנבנה מחדש אין סיבה לשמור בגיט, ובמקרה הזה יש סיבה טובה לא לשמור.

כשמתעדים שדה שדורש כתובת — מתארים את השדה ואת תפקידו, בלי לכתוב ערך אמיתי.

### לפני כל קומיט

לבדוק מה נכנס בפועל. אם נוסף פרט אישי — לעצור לפני הדחיפה, לא אחריה:
היסטוריית git ציבורית בדיוק כמו הקבצים עצמם, ומחיקה מהגרסה הנוכחית לא מוחקת אותה מההיסטוריה.

## סודות

מפתח ה-API של Gemini וסיסמת ה-SMTP **לא נמצאים בשום קובץ בפרויקט.**
שניהם נשמרים ב-Credentials של n8n, שיושבים בבסיס נתונים מקומי מוצפן.
