# מילון מונחים

כל מילה שתשמע ממני בדרך. אין צורך לשנן — תחזור לכאן כשתיתקל במשהו.

---

## הבסיס

**אוטומציה / Workflow / Scenario**
שרשרת של פעולות שרצה לבד. כמו קו ייצור: כל תחנה מקבלת משהו, עושה משהו, ומעבירה הלאה.

**טריגר (Trigger)**
מה שמפעיל את השרשרת. "עלה פרק חדש", "הגיעה שעה 8:00", "לחצתי כפתור".
לכל אוטומציה יש **טריגר אחד בלבד**, והוא תמיד ראשון.

**פעולה (Action) / מודול / Node**
תחנה בשרשרת. "שלח מייל", "קרא לשירות X", "שמור קובץ".

**הרצה (Run / Execution)**
פעם אחת שהשרשרת רצה מהתחלה לסוף.

**מיפוי (Mapping)**
להגיד לתחנה: "קח את התוצאה של התחנה הקודמת ושים אותה כאן".
בפועל זה גרירה של תגית קטנה. זו הפעולה שתעשה הכי הרבה פעמים.

---

## תקשורת בין שירותים

**API**
הדלת האחורית של שירות — הדרך שבה תוכנה מדברת עם תוכנה אחרת, בלי בן אדם ובלי מסך.
אתר שיש לו API = אפשר לחבר לאוטומציה. אתר בלי API = אפשר רק ידנית.

**מפתח API (API Key)**
סיסמה ארוכה שמזהה אותך מול השירות. בפועל: מעתיקים מאתר אחד, מדביקים באחר.
**לא משתפים אותה עם אף אחד** ולא שמים אותה בקובץ פומבי.

**בקשת HTTP (Request)**
הודעה שנשלחת לשירות. מורכבת מ:
- **כתובת (URL)** — לאן
- **שיטה (Method)** — `GET` = תן לי מידע, `POST` = קח ממני מידע
- **Headers** — "פרטי המעטפה", בדרך כלל שם המפתח
- **Body** — התוכן עצמו

**תשובה (Response) וקודי סטטוס**
- `200` — הצליח
- `401` / `403` — המפתח שגוי או חסר הרשאה
- `404` — הכתובת לא קיימת
- `429` — שלחת יותר מדי בקשות, האט
- `500` — נפל אצלם, לא אצלך

**JSON**
הפורמט שבו שירותים מדברים ביניהם. נראה ככה:
```json
{ "title": "פרק 42", "duration": 3600 }
```
כל דבר הוא `"שם": ערך`. סוגריים מסולסלים = אובייקט, מרובעים = רשימה.
אתה לא צריך לכתוב את זה — רק לדעת לקרוא ולמשוך ממנו שדה.

**Webhook**
כתובת שהמערכת שלך נותנת לשירות חיצוני, כדי שהוא יקרא **אליה** כשקורה משהו.
ההפך מ-API רגיל: במקום שאתה תשאל, הם מודיעים.

**Polling**
לשאול שוב ושוב "נו, סיימת?" כל כמה שניות. פחות אלגנטי מ-webhook, לפעמים הכרחי.

---

## פודקאסטים

**RSS Feed**
קובץ שכל פודקאסט מפרסם, ובו רשימת כל הפרקים. זה מה שכל אפליקציית פודקאסטים בעולם קוראת.
זה גם המקור הכי אמין לטריגר "יצא פרק חדש".

**Enclosure**
השדה בתוך ה-RSS שמכיל את **הקישור הישיר לקובץ ה-MP3**. זה החלק שבאמת מעניין אותנו — בלעדיו אין מה לתמלל.

---

## AI

**מודל (Model)**
ה-AI עצמו. לכל ספק יש כמה, בגדלים ומחירים שונים.

**פרומפט (Prompt)**
ההוראה שאתה נותן ל-AI. **זה החלק שקובע את איכות התוצאה יותר מכל דבר אחר.**

**טוקן (Token)**
יחידת המידה של טקסט מול AI. בערך 3-4 אותיות. בעברית זה פחות יעיל מאנגלית.
חשוב כי: מחיר נמדד בטוקנים, ויש מגבלה כמה טוקנים נכנסים בבת אחת.

**חלון הקשר (Context Window)**
כמה טקסט המודל יכול לקרוא בבקשה אחת. תמלול של שעתיים עלול לא להיכנס — נפתור את זה בשיעור 5.

**הזיה (Hallucination)**
כשה-AI ממציא בביטחון. נצמצם את זה בעזרת פרומפט טוב ובדיקה מול התמלול.

---

## מגבלות ומכסות

**Operation / Task / Credit**
היחידה שלפיה סופרים לך שימוש בפלטפורמת האוטומציה. **בדרך כלל כל תחנה בשרשרת = יחידה אחת.**
זאת אומרת: אוטומציה של 6 תחנות שרצה 10 פעמים = 60 יחידות, לא 10.

**Rate Limit**
כמה בקשות מותר לשלוח בזמן נתון. מעבר לזה תקבל `429`.

**Free Tier**
המכסה החינמית. המספרים משתנים כל הזמן — נאמת אותם ביחד בשיעור 1.
